{
  "name": "cinecritique-project",
  "version": "1.0.0",
  "description": "CineCritique - Système de critiques de films",
  "scripts": {
    "install:all": "npm install && cd cinecritique-backend && npm install && cd ../cinecritique-frontend-vite && npm install",
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "cd cinecritique-backend && npm run dev",
    "dev:frontend": "cd cinecritique-frontend-vite && npm run dev",
    "build": "cd cinecritique-frontend-vite && npm run build"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
